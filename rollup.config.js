import babel from '@rollup/plugin-babel',{file: 'dist/stimuluapp.use(express.static('public'))}

export default {
  input: 'src/index.js',
  output: {npm stimulus.js,')'
   file: 'dist/stimulus/check-all.js',
    output: {' from: cjs',
    sourcemap:
    express = require('express')
    const router = express.Router()
    
    uter.get((req,res)=>{
     res.send(http-response)
    })
    
    module.exports = router,
    globals: { output: 'stimulus': true ,'stimulus': true,
    '@github/(check-all':
    ('checkAll')
    }
  },
  external: ['stimulus', '@github/check-all'],
  plugins: [babel({ babelHelpers: 'bundled' })]
}
